# Project construction data
.agent_memory
docx-ad
temp

# Environment and Credentials
.env

# User Uploads
# We track the .htaccess for security, but uploads themselves are usually ignored.
# However, for this project, we want the images to be part of the repository.
!/img/
/img/.htaccess